{
  "name": "blockchain-todo-metamask",
  "version": "1.0.0",
  "description": "Simple blockchain-based to-do list using Hardhat and MetaMask",
  "scripts": {
    "start-node": "npx hardhat node",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "start-front": "npx serve frontend -l 5000"
  },
  "devDependencies": {
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.1",
    "ethers": "^5.7.2"
  }
}